/* iCITY 113Н — обёртка посадочной страницы.
   Путь в проекте: app/(landing)/layout.tsx

   ЗАЧЕМ ГРУППА МАРШРУТОВ. Прелоадер — экран 0 ИМЕННО ЭТОЙ страницы:
   лифт едет по реальным байтам двух видео первого экрана, а створки
   открывают hero. На /privacy ни того, ни другого нет, и пока прелоадер
   стоял в корневом layout, юридический документ открывался через три
   секунды лифта, а startHeroPreload() успевал утащить ~3 МБ видео,
   которое на той странице никто не посмотрит.

   Группа (landing) в адрес не попадает: «/» остаётся «/». Порядок узлов
   в <body> тоже прежний — Preloader первым, за ним контент, затем Cursor
   и SmoothScroll из корневого layout. То есть для посадочной страницы
   не меняется ничего, а /privacy просто перестаёт быть её частью.

   Cursor и SmoothScroll намеренно ОСТАЛИСЬ в корне: свой курсор и
   плавная прокрутка уместны и на длинном документе, оба сами решают,
   работать ли (тач, prefers-reduced-motion). Сюда спускается только то,
   что привязано к hero. */

import Preloader from '@/components/Preloader';

export default function LandingLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <>
      <Preloader />
      {children}
    </>
  );
}
