/* iCITY 113Н — географические факты локации.
   Путь в проекте: lib/geo.ts

   Порядок пары — [долгота, широта]. Так их принимает Яндекс JS API v3
   (LngLat), и переставлять их нельзя нигде: та же дисциплина, что
   у плана этажа в PlanScene.tsx, только другая пара.

   ОТКУДА ЧИСЛА. Точка комплекса — из ссылки Яндекс Карт, которую дал
   заказчик (poi[point] в yandex.ru/maps/-/CTTVAU6r). Станции и ТТК —
   из OpenStreetMap через Overpass, узлы railway=halt / subway_entrance
   / highway=primary, отобранные по расстоянию от комплекса. Расстояния
   в комментариях посчитаны там же и сходятся с docs/facts.md: до МЦД
   105 м — это и есть «1 минута пешком», до Шелепихи 368–433 м — пять.

   Названия станций. МЦД-1 «Тестовскую» переименовали в «Москва-Сити»;
   Яндекс подписывает её «Москва-Сити (Тестовская)». В тексте секции
   осознанно оставлено прежнее имя — по нему объект ищут, — но метка
   на карте несёт оба. */

/** [долгота, широта] — порядок ymaps3, не менять местами. */
export type LngLat = [number, number];

/** Space Tower, помещение 113Н. Точка из ссылки заказчика. */
export const ICITY: LngLat = [37.530713, 55.754630];

/** МЦД-1 «Москва-Сити» (быв. «Тестовская»), северная платформа. 105 м. */
export const MCD: LngLat = [37.531511, 55.753799];

/** «Шелепиха»: вестибюль метро, ближайший вход. 368 м.
    Платформа МЦК — [37.525668, 55.757306], на 85 м севернее. */
export const SHELEPIKHA: LngLat = [37.525950, 55.756579];

/** Ближайшая проезжая часть ТТК восточнее комплекса. 130 м. */
export const TTK: LngLat = [37.532743, 55.754890];

/* Центр и зум подобраны так, чтобы все четыре точки попадали в кадр
   с запасом: разброс 437 м по долготе и 387 м по широте, на зуме 16,3
   это примерно 0,95 м на пиксель. */
/* Центр доведён по замеру на живой карте: содержимое стояло на 43 px
   правее и 31 px ниже середины рамки — отсюда +0,00075° долготы
   и −0,0003° широты. */
export const MAP_CENTER: LngLat = [37.52995, 55.75525];
export const MAP_ZOOM = 16.3;
